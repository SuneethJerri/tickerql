__pycache__/
*.py[cod]
.venv/
venv/
# Every dotenv variant is a secret, EXCEPT the committed templates.
# Previously this listed only .env and .env.local, which left .env.neon,
# .env.production and friends committable.
.env*
!.env.example
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
node_modules/
web/dist/
.DS_Store
*.log
.ingest_checkpoints/

# build artifacts
*.tsbuildinfo

# Agent tooling. Local editor/assistant config, not project source - it was
# tracked for the first 59 commits, which made the repo read as a Claude
# project in the GitHub file listing. The files stay on disk; they just are
# not the repo's business.
.claude/
CLAUDE.md

# Screenshot pass output. web/scripts/screenshot.py writes 24+ PNGs per run.
web/screenshots/
